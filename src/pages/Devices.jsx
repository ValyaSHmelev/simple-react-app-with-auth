import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Devices = () => {
  return (
    <div>
      <Title level={2}>Приборы</Title>
    </div>
  );
};

export default Devices;
